# TODO: Multi-Invoice Tracker & Column Restructure

## Overview
Restructure KC PP Sync sheet from single-invoice-per-row to multi-invoice tracking per Jobber Job #, with full client invoice visibility and automatic "All Paid?" roll-up. Applies to all monthly one-off tabs. Recurring tabs (` - R`) are NOT affected by column restructure — they keep current layout.

## Current State
- **Sheet ID:** `1p4lxIUjWFYNDp6ptqSMwyRcdle5Hcv5UMC6TdpZE99Q`
- **Current columns:** A–Z (26 columns)
- **Target columns:** A–AM (39 columns)

### Tab Naming Convention (NEW)
Going forward, every month gets exactly 3 tabs:

| Tab Name | Purpose | Example |
|---|---|---|
| **[Month]** | One-off jobs, full auto invoice tracker | `March` |
| **[Month] - R** | Recurring visits, manual invoice entry | `March - R` |
| **[Month] - GTP $** | Good to Pay summary | `March - GTP $` |

Short names. No year suffix unless needed for disambiguation (e.g., crossing into a new year).

### Tab Renames Required
| Current Name | New Name |
|---|---|
| `March 2026` | `March` |
| `EFRAIN - R` | `March - R` (merge EFRAIN - R + JASON - R data into single recurring tab) |
| `JASON - R` | _(merged into March - R, then deleted)_ |
| `GTP $ - March` | `March - GTP $` |

**January 2026 and February 2026 are NOT touched.** They keep their current name and A–Z layout. This work is March-forward only.

### Tabs Affected by Column Restructure
- **March** (and all future monthly one-off tabs) → new A–AM layout
- **January 2026, February 2026** → NO CHANGES, legacy layout preserved
- **Recurring tabs (` - R`):** Keep current A–Z layout — NOT affected by column restructure
- **GTP $ tabs:** Update source column references only

## Data Preservation — CRITICAL
These columns contain manually entered finance data that MUST be preserved through the migration:

| Current Col | Header | Rows with data (March) | Notes |
|---|---|---|---|
| B | REVIEW | 201/202 | Boolean flags |
| S | KCPC Released Amount | 136/202 | Dollar amounts |
| U | Payment Status | 201/202 | Dropdown: Good to Pay, NO CLIENT PAY, On Hold, Pending Approval in HP |
| V | Payment Tracking (Finance) | 201/202 | Dropdown: PAID, AWAITING FOR PAYMENT |
| W | Payment Method (Finance) | 136/202 | Dropdown: QBO-Billpay- ACH/Check, ACH - 9292 |
| X | Date of Payment | 136/202 | Date values |
| Y | NOTES / REMARKS | 26/202 | Free text notes |

**Migration rule:** Read ALL current data from old positions → write to new positions. No data loss.

---

## Column Map: Old → New

### Unchanged (A–K)
| Col | Header | Change |
|---|---|---|
| A | Date | No change |
| B | REVIEW | No change |
| C | Company Name | No change |
| D | Preferred Partner Owner Name | No change |
| E | HeyPros ID # | No change |
| F | Job # | No change |
| G | Jobber Link | No change |
| H | Job Status | No change |
| I | Job Type | No change |
| J | Client Name | No change |
| K | Division | No change |

### Replaced (L–N) — OLD: single invoice → NEW: invoice summary
| New Col | New Header | Old Col | Old Header | Source |
|---|---|---|---|---|
| L | # of Invoices | L | Invoice Number | Auto (Jobber) — count of all invoices for this Job # |
| M | Total Invoiced | M | Jobber Invoice Total Amount | Auto (Jobber) — sum of all invoice amounts |
| N | All Paid? | N | Invoice Issued Date | Auto (Jobber) — ✅ if all paid, ❌ if any unpaid |

### Removed
| Old Col | Old Header | Disposition |
|---|---|---|
| O | Jobber Invoice Status | Moved to tracker block (per-invoice status) |
| P | Date Invoice Paid | Moved to tracker block (captured in status) |

### Shifted -2 (Old Q–Z → New O–X)
| New Col | Header | Old Col | Data Migration |
|---|---|---|---|
| O | HeyPros Invoice # | Q | Auto — no manual data |
| P | Sub Invoice Amount | R | Auto — no manual data |
| **Q** | **KCPC Released Amount** | **S** | **⚠️ MANUAL DATA — 136 rows, must migrate** |
| R | Contractor Invoice PDF | T | Auto — no manual data |
| **S** | **Payment Status** | **U** | **⚠️ MANUAL DATA — 201 rows, must migrate + update dropdown** |
| **T** | **Payment Tracking (Finance)** | **V** | **⚠️ MANUAL DATA — 201 rows, must migrate + update dropdown** |
| **U** | **Payment Method (Finance)** | **W** | **⚠️ MANUAL DATA — 136 rows, must migrate + update dropdown** |
| **V** | **Date of Payment** | **X** | **⚠️ MANUAL DATA — 136 rows, must migrate** |
| **W** | **Notes / Remarks** | **Y** | **⚠️ MANUAL DATA — 26 rows, must migrate** |
| X | Auto Notes | Z | Auto — regenerated by sync |

### New (Y–AM) — Invoice Tracker Block (5 slots × 3 cols)
| Slot | Invoice # | Amount | Paid? |
|---|---|---|---|
| 1 | Y | Z | AA |
| 2 | AB | AC | AD |
| 3 | AE | AF | AG |
| 4 | AH | AI | AJ |
| 5 | AK | AL | AM |

All auto-populated from Jobber API. No manual input.

---

## Phases

### Phase 0: Tab Renames & Recurring Merge
**Goal:** Rename all tabs to new naming convention and consolidate recurring data.

**Tasks:**
- [x] 0.1 Rename `March 2026` → `March`
- [x] 0.2 Merge EFRAIN - R + JASON - R into single `March - R` tab (44 rows, 42 Efrain + 2 Jason)
- [x] 0.3 Rename `GTP $ - March` → `March - GTP $`
- [x] 0.4 Delete old `EFRAIN - R` and `JASON - R` tabs after merge verified
- [ ] 0.5 Update sync code tab detection:
  - `isRecurringTab` check: still detects ` - R` suffix ✅ (no change needed)
  - `parseTabMonth()`: update to parse `March` (no year) in addition to `March 2026` (backward compat for Jan/Feb)
  - GTP tab name pattern: update from `GTP $ - March` to `March - GTP $`
  - Default tab config: update to new naming
- [ ] 0.6 Update Cloud Scheduler job payloads to use new tab names

**Gate:**
- [ ] All tabs renamed correctly (visual check in sheet)
- [ ] March - R has all data from EFRAIN - R + JASON - R combined
- [ ] Sync runs successfully with new tab names (test March, March - R, March - GTP $)
- [ ] No broken references or orphaned tabs

---

### Phase 1: Data Migration Script
**Goal:** Safely migrate manual data from old column positions to new positions on all monthly tabs.

**Tasks:**
- [x] 1.1 On **March** tab only:
  - Read entire sheet data (A:Z)
  - Extract manual/finance columns at old positions: S(18), U(20), V(21), W(22), X(23), Y(24)
  - Write extracted data to new positions: Q(16), S(18→new), T(19→new), U(20→new), V(21→new), W(23→new)
  - **Column shift math:**
    - Old S (col index 18) → New Q (col index 16): shift LEFT 2
    - Old U (col index 20) → New S (col index 18): shift LEFT 2
    - Old V (col index 21) → New T (col index 19): shift LEFT 2
    - Old W (col index 22) → New U (col index 20): shift LEFT 2
    - Old X (col index 23) → New V (col index 21): shift LEFT 2
    - Old Y (col index 24) → New W (col index 22): shift LEFT 2
- [x] 1.2 Clear old column positions (full sheet rewrite)
- [x] 1.3 Update headers row on March tab to new schema (39 cols A–AM)
- [x] 1.4 Add new headers for Y–AM (Invoice Tracker block)
- **Note:** January and February tabs are NOT migrated — they keep their current layout.

**Gate:**
- [ ] Verify row counts match before/after for every finance column on March
- [ ] Spot-check 5 random rows on March: old value at old position = new value at new position
- [ ] No data in columns beyond AM (nothing pushed off-sheet)
- [ ] January 2026 and February 2026 tabs completely untouched

**Abort if:** Any finance column row count drops during migration.

### Phase 2: Dropdown & Formatting Updates
**Goal:** Update data validation dropdowns and conditional formatting to new column positions.

**Tasks:**
- [x] 2.1 Delete old dropdown validations on old columns (V, W cleared)
- [x] 2.2 Create new dropdown validations:
  - S (Payment Status): Good to Pay, NO CLIENT PAY, On Hold, Pending Approval in HP
  - T (Payment Tracking): PAID, AWAITING FOR PAYMENT
  - U (Payment Method): QBO-Billpay- ACH/Check, ACH - 9292
  - H (Job Status): existing values — verify position unchanged
  - I (Job Type): existing values — verify position unchanged
  - K (Division): existing values — verify position unchanged
  - N (All Paid?): ✅, ❌ (new — or auto-populated text, no dropdown needed)
- [x] 2.3 Update conditional formatting rules (38 rules rebuilt at new positions):
  - Delete all existing CF rules (they reference old column positions)
  - Recreate CF rules mapped to new column positions
  - Key CF rules: Payment Status colors, Payment Tracking colors, Review flag, Invoice Status indicators
- [x] 2.4 Format new columns:
  - L (# of Invoices): number format, centered
  - M (Total Invoiced): currency ($#,##0.00)
  - N (All Paid?): centered
  - Y–AM: Invoice # = text, Amount = currency, Paid = centered
- [x] 2.5 Apply formatting to March tab only (Jan/Feb untouched)

**Gate:**
- [ ] Dropdowns work on new columns (test manual entry on a scratch row)
- [ ] CF rules fire correctly (visually verify on March tab)

### Phase 3: Sync Code — Column Map Update
**Goal:** Update all hardcoded column letter references in the sync function.

**Tasks:**
- [ ] 3.1 Update `function.ts` — column letter assignments in `values` object:
  - Old: L, M, N, O, P, Q, R, S, T, Z
  - New: O (was Q), P (was R), R (was T), X (was Z)
  - Remove: L, M, N, O, P writes (old invoice columns) — replaced by new logic
  - Add: L, M, N writes (new invoice summary)
  - Add: Y–AM writes (tracker block)
- [ ] 3.2 Update `sheets.ts`:
  - `AUTO_COL_LETTERS` set — update all letter references
  - `readOutputSheetJobNumbers()` — update column index for Job # (F stays at 5, OK) and `existingInvoiceValue` (old L → remove or repurpose)
  - `batchUpdateAutoColumns()` — update column letter mapping
  - Manual/finance column protection list — update to new positions (Q, S, T, U, V, W)
- [ ] 3.3 Update `constants.ts` if any column constants exist
- [ ] 3.4 Remove `existingInvoiceValue` / manual hold logic for column L (L is now auto — # of invoices)
  - Manual hold (`-` in Invoice #) needs to move to a different mechanism or be removed
  - **Decision needed:** Is manual hold still relevant with multi-invoice tracking? If yes, where does the flag live?
- [ ] 3.5 Update shared invoice detection (step 5a) — may not be needed anymore since tracker block shows all invoices explicitly

**Gate:**
- [ ] TypeScript compiles with zero errors
- [ ] Dry run on March tab produces correct column letter mappings
- [ ] No writes to old column positions

### Phase 4: Sync Code — Multi-Invoice Jobber Query
**Goal:** Fetch ALL invoices per Job # from Jobber API (not just highest invoice number).

**Tasks:**
- [ ] 4.1 Update Jobber adapter (`jobber.ts`):
  - Current: picks single invoice (highest number) per job
  - New: return ALL invoices per job in the response
  - Query must include: invoiceNumber, total/amount, invoiceStatus, issuedDate, paidDate, webUri
  - Sort invoices by invoiceNumber ascending (slot 1 = first invoice)
- [ ] 4.2 Update types (`types.ts`):
  - Add `allInvoices: JobberInvoice[]` to JobberPaidJob or create new return type
  - JobberInvoice type: { invoiceNumber, amount, invoiceStatus, issuedDate, paidDate, webUri }
- [ ] 4.3 Update `function.ts` to use full invoice list:
  - L = allInvoices.length
  - M = sum of all invoice amounts (formatted as currency)
  - N = ✅ if every invoice status = "paid", else ❌
  - Y–AM: fill tracker slots with each invoice's number, amount, status
  - Job-level data (L, M, N, tracker block) should be IDENTICAL across all rows sharing the same Job #
- [ ] 4.4 Handle edge cases:
  - Job with 0 invoices: L=0, M=blank, N=blank, tracker empty
  - Job with >5 invoices: fill first 5 slots, auto-note warns about overflow
  - Draft invoices: include or exclude? **Decision: include with "Draft" status in tracker**

**Gate:**
- [ ] Test with known multi-invoice jobs (#14665: 5 invoices, #19422: 4 invoices)
- [ ] Verify L/M/N values match Jobber dashboard
- [ ] Verify tracker block shows correct invoice #, amount, status per slot
- [ ] Single-invoice jobs (90%+): slot 1 filled, slots 2–5 empty

### Phase 5: Auto Notes Update
**Goal:** Update auto notes for new column position (X) and add multi-invoice conditions.

**Tasks:**
- [ ] 5.1 Keep existing notes 1–11 (update column references only)
- [ ] 5.2 Add new notes:
  - **12: Multiple client invoices** — `ℹ️ 3 client invoices (total $7,725)`
  - **13: Partial client payment** — `🔴 2 of 3 client invoices unpaid`
  - **14: All paid, PP unpaid** — `✅ All client invoices paid — ready for PP payment`
  - **15: Invoice >5 overflow** — `⚠️ Job has 6+ invoices — only first 5 shown in tracker`
- [ ] 5.3 Remove or update shared invoice note (#11) — tracker block may make it redundant
- [ ] 5.4 Future notes (not this phase, just document):
  - High cost ratio (>50%)
  - Missing sub invoice (P empty)
  - Job archived, client unpaid
  - Recurring job type on one-off tab
  - PP payment on hold >7 days
  - Client invoice past due >14 days

**Gate:**
- [ ] Auto notes appear correctly on March tab after sync
- [ ] No stale notes referencing old column positions

### Phase 6: GTP $ Tab Update
**Goal:** Update GTP $ tab refresh logic for new column positions.

**Tasks:**
- [ ] 6.1 Update `refreshGTPTab()` in `sheets.ts`:
  - Source columns for GTP filter criteria shift:
    - Old O (Jobber Invoice Status) → New N (All Paid?) — filter by ✅ instead of "Paid"
    - Old U (Payment Status) → New S — "Good to Pay"
    - Old V (Payment Tracking) → New T — "AWAITING FOR PAYMENT"
  - Output columns may also need adjustment depending on what GTP tab shows
- [ ] 6.2 Update GTP tab headers if they reference source column names
- [ ] 6.3 Test GTP refresh on March after sync

**Gate:**
- [ ] GTP $ tab row count matches expected (current: ~25-31 rows for March)
- [ ] Filter criteria produce correct results with new column positions

### Phase 7: Deploy & Validate
**Goal:** Deploy updated Cloud Function and validate on all tabs.

**Tasks:**
- [ ] 7.1 Build TypeScript (`npm run build`)
- [ ] 7.2 Deploy to Cloud Run (`gcloud functions deploy`)
- [ ] 7.3 Run sync on March — verify:
  - L/M/N populated correctly
  - Tracker block (Y–AM) populated correctly
  - Finance columns (Q, S, T, U, V, W) preserved with correct data
  - Auto notes in X (not Z)
  - March - GTP $ refreshed correctly
- [ ] 7.4 Run sync on January 2026 and February 2026 — verify they still work with OLD layout (backward compat)
- [ ] 7.5 Run sync on March - R — verify recurring tab unaffected
- [ ] 7.6 Spot-check 10 rows on March:
  - Compare L (# invoices) against Jobber dashboard
  - Compare M (total invoiced) against Jobber dashboard
  - Compare N (all paid?) against actual invoice statuses
  - Verify finance data matches pre-migration snapshot

**Gate:**
- [ ] March syncs clean with new layout, no errors
- [ ] Jan/Feb sync clean with old layout (backward compat)
- [ ] March - R syncs clean and unchanged
- [ ] Finance data integrity verified (row counts match Phase 1 gate)
- [ ] Nathan visual review on March tab

---

## Open Decisions
1. **Manual hold mechanism:** Old L = `-` trick no longer works since L is now "# of Invoices". Where does manual hold flag live? Options: (a) Notes column, (b) new flag in Payment Status dropdown, (c) remove entirely since tracker block shows invoice-level status
2. **Recurring tab column layout:** Keep current A–Z — confirmed. Recurring uses different invoice logic anyway.
3. ~~Historical tabs~~ **DECIDED:** March-forward only. January 2026 and February 2026 keep their current name and layout.
4. **Draft invoices in tracker:** Show drafts in tracker slots or exclude? Suggested: include with "Draft" label.
5. **Backward compat:** Sync code must detect tab naming format — new (`March`) vs old (`January 2026`) — and use the correct column layout for each. Two code paths or config flag.

## Risk
- **Data loss during column shift** — mitigated by Phase 1 gate (row count verification + spot checks)
- **Sync overwrites finance data** — mitigated by keeping finance columns in the "manual/protected" list
- **Breaking GTP $ tab** — mitigated by Phase 6 dedicated update
- **Recurring tab breakage** — mitigated by NOT changing recurring tab layout

## Estimated Effort
- Phase 0 (renames + merge): 20 min
- Phase 1 (migration): 30 min
- Phase 2 (formatting): 20 min
- Phase 3 (code column map): 30 min
- Phase 4 (multi-invoice query): 45 min
- Phase 5 (auto notes): 15 min
- Phase 6 (GTP tab): 15 min
- Phase 7 (deploy + validate): 30 min
- **Total: ~3.5 hours**
